<div class="container">
  <%= simple_form_for [current_user, @venue] do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :overview %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Register my venue", class: "btn btn-warning"%>
  <% end %>
</div>
